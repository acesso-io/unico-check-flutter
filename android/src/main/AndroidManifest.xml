<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.unico_check">

    <application>

        <activity
            android:name=".UnicoCheckActivityDocument"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".UnicoCheckActivityCameraActivity"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>
