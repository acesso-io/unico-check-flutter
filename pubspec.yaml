name: unico_check
description: Esta biblioteca visa implementar a tecnologia Unico.
version: 0.1.0
homepage: https://github.com/acesso-io/unico_check_flutter

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner: ^1.12.2
  json_serializable: ^4.1.0

flutter:
  plugin:
    platforms:
      android:
        package: com.unico_check
        pluginClass: UnicoCheckPlugin
      ios:
        pluginClass: UnicoCheckPlugin